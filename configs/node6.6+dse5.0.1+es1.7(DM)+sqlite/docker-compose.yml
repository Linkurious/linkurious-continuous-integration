version: '2'
services:
  linkurious:
    build:
      context: .
      args:
        ci_directory: ${CI_DIRECTORY}
    depends_on:
     - datastax
     - elasticsearch
  datastax:
    build:
      context: ./dse
  elasticsearch:
    image: elasticsearch:1.7
